# Refactor plan — `src/components/GameStage.tsx` in `TeacherEvan/Worm-ranch`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-08T09:52:05.957465+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/Worm-ranch` |
| path | `src/components/GameStage.tsx` |
| size | 18.3 KB (18,694 bytes) |
| lines | 478 |
| generated | 2026-09-08T09:52:05.957481+07:00 |

### Top imports (sample)
  - `import { useEffect, useId, useMemo, useRef, useState } from "react";`
  - `import styles from "./GameStage.module.css";`
  - `import { createGameStageAudioController } from "@/components/gameStageAudio";`
  - `import { GameStageOverlay } from "./GameStageOverlay";`
  - `import {`
  - `import { getTargetCallout } from "./gameStageTargetCallout";`
  - `import { useLatestValue, useStageActionEcho } from "./gameStageActionEcho";`
  - `import { getKeyboardStatus, getKeyboardTargetId, type KeyboardTargetMode } from "@/components/gameStageKeyboard";`
  - `import {`
  - `import { getCueEffect, getMotionFeedback, type StageMotionCue } from "@/components/gameStageMotion";`

## Structural analysis

**Top-level exports:**
- `GameStage` (L64)

**React hook uses:**
- `levelRules` = useMemo (L74)
- `canvasRef` = useRef (L84)
- `worldRef` = useRef (L86)
- `frameRef` = useRef (L87)
- `summaryRef` = useRef (L88)
- `feedbackRef` = useRef (L89)
- `feedbackIdRef` = useRef (L90)
- `particlesRef` = useRef (L91)

**Top-level consts:**
- `SUMMARY_INTERVAL_MS` (L62)

**Render branches:** 2 `return (` blocks at L418, L440

**Repeated literals (≥2×):**
- ` || result.kind === ` (×4)

**Top-referenced imports:**
- `useRef` (20 refs)
- `getKeyboardTargetId` (7 refs)
- `useLatestValue` (6 refs)
- `useState` (5 refs)
- `useEffect` (4 refs)
- `useId` (3 refs)
- `GameStageOverlay` (3 refs)
- `getMotionFeedback` (3 refs)
- `useMemo` (2 refs)
- `createGameStageAudioController` (2 refs)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `Worm-ranch`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `' || result.kind === '` (×4) to a constant
- **Target:** String literal appears 4× in `src/components/GameStage.tsx`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `src/components/GameStage.tsx` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' src/components/GameStage.tsx` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Confirm `useRef` (20 refs) is the right shared utility
- **Target:** `useRef` is the most-referenced import in this file (20 uses).
- **Action:** verify whether other dashboard pages also import it; if so, ensure it lives
  in a shared `lib/` or `@/components/ui/` and not in a feature-local path.
- **Acceptance:** document the canonical home for `useRef` in `docs/`.
- **Validation:** `rg -l 'useRef' src/` — should match ≤3 call sites in
  feature code, more in shared.
- **Evidence:** call-site map.

### OBJ-003 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/components/GameStage.tsx` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/components/GameStage.tsx`.
- **Validation:** `rg -n '^import' src/components/GameStage.tsx` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-004 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/components/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-005 — Reduce `src/components/GameStage.tsx` below 239 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/components/GameStage.tsx` shrinks from 478 to ≤239 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-006 — Audit Convex/RTK/Query calls for duplicate fetch
- **Target:** `useQuery(api.*)` (or equivalent state-fetch hook) calls in `src/components/GameStage.tsx`.
- **Why:** Multiple identical queries in the same render path duplicate DB load.
- **Acceptance:** distinct query keys are minimized; shared queries lifted
  to a parent hook or memoized.
- **Validation:** `rg -n 'useQuery\(' src/components/GameStage.tsx` cross-checked with the convex dashboard.
- **Evidence:** query-count diff + Convex dashboard before/after.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/components/GameStage.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/components/GameStage.tsx` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `Worm-ranch`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-08-bigfile-Worm-ranch-GameStage.tsx.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-08T10:23:04.827128+07:00

**Verdict:** `READY-WITH-WARNINGS`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. llm_unreachable
